const express = require("express");
const router = express.Router();

// Note: Auth endpoints (signup, login) are defined directly in server.js
// This file can be used for future auth routes like password reset, etc.

module.exports = router;